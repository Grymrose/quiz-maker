#include <iostream>
#include <string>
#include <vector>

class QuizSession{
    private:
    Quiz Quiz;
    vector <String> Answers;
    int Score;
    int TimeElapsed;

    public:
    void vector<String> answers SubmitAnswer;
    int GetScore();
    int GetTimeElapsed();
}